# 구현 전 원본 확인 A–F

확인한 정본: `/Users/minah/Desktop/K-ds/예선/6. 한국산림복지진흥원`.
이 문서의 원본 경로는 이 디렉터리 기준 상대 경로입니다. 구현 전 코드·전처리 자료·성과 CSV를 확인한 결과와 이후 저장한 객체를 구분합니다.

## A. GAP 활동추천 모델

- 코드: `분석산출물/gap_target_results/gap_target_run.py`.
- 모델: 라벨별 CatBoost, depth 6 / 360 iterations / Ordered / seed 123 / learning rate 0.05 / Balanced class weights.
- 입력: `분석산출물/catboost_tuning_results_raw515/_prep_artifacts/{X.parquet,Y.parquet,meta.json}`. 원래 478열에서 Q20_, Q19_5, Q21, Q21_1의 69열을 제외한 **409열**. Q17 타깃 문항은 원 전처리에서 이미 제외돼 있습니다.
- 인구통계 입력: 출생연도 SQ7_1, 연령대 D_SQ7, 성별 SQ6, 가구 SQ3/D_SQ3, 혼인 DQ3, 소득 DQ5/D_DQ5, 학력 DQ2, 직업 DQ1/DQ1_1/DQ1_2, 지역 CO11/CO12/CO13/D_CO11. Q10 경험 등 다른 설문 입력도 사용하는 모델입니다.
- 출력 순서: 등산·트레킹형, 자연감상·산책형, 캠핑·야영형, 체험·학습형, 치유·웰니스형, 레포츠·모험형.
- 타깃: 각 6대분류별 **Q17 의향 있음 AND Q10_1/Q10_2 경험 없음**. 단순 의향에서 수치 뺄셈을 하는 것이 아니라 두 조건의 논리 결합입니다. 대분류 내 세부활동 하나라도 경험하면 해당 대분류는 신규 타깃에서 제외됩니다.
- 학습 표본 7,626명 / 가구그룹 3,591개. 기존 가구그룹 5-fold OOF 결과는 `gap_summary.csv`, `gap_perlabel_pooled.csv`에 있습니다.

| 지표 | 기존 결과 |
|---|---:|
| P@1 | 0.680845 |
| P@2 | 0.554617 |
| LRAP | 0.843106 |
| Macro-F1 | 0.590552 |
| Macro PR-AUC: fold 평균 | 0.585397 |
| Macro PR-AUC: pooled OOF | 0.581977 |
| 인기순 P@1 | 0.375371 |
| 경험 제외 인기순 P@1 | 0.425887 |

P@k는 양성 GAP 활동이 있는 5,579명을 대상으로 하며 2,047명은 제외합니다. 인구통계만 입력하는 화면의 성능이나 저장된 전체 학습 모델의 독립 테스트 성능으로 표기하지 않습니다.

기존 실행 코드와 OOF 출력은 확인했으나 앱에서 직접 읽을 최종 `.cbm` 묶음은 작업 시작 시 발견하지 못했습니다. 기존 타깃과 설정을 그대로 사용한 일회성 전체 자료 학습으로 6개 모델을 저장했습니다. 의향 모델의 예측을 GAP 예측으로 바꿔 표시하지 않습니다.

## B. 시설추천 모델

- 전처리: `분석산출물/catboost_tuning_results_raw515_facility/_prep_artifacts/`, 11,437명 × 498열, Q20_5 타깃 입력 제외.
- 저장용 설정 출처: `분석산출물/personal_recommender/build_recommender_tuned.py`의 `FAC_PARAMS`. CatBoost depth 7 / 880 iterations / Plain / seed 42 / learning rate 0.05 / Balanced class weights.
- 출력 13개: 자연휴양림, 산림욕장, 치유의숲, 숲길, 유아숲체험원, 산림교육센터, 수목장림, 숲속야영장, 산림레포츠시설, 산림복지단지, 수목원, 정원, 목재문화체험장.
- 성능 출처: `분석산출물/catboost_tuning_results_raw515_facility/ranking_metrics.csv`의 기존 CV 평균. Macro-F1 0.660407 / LRAP 0.826969 / P@2 0.720070.
- 저장된 모델 객체는 시작 시 발견하지 못해 위 설정으로 13개 모델을 일회성 저장했습니다. 원 빌더의 인구통계별 평균 테이블과 계절 승수는 개인 모델 추론에 사용하지 않습니다.

## C. 지역 수요·공급 자료

- 최종 예산 최적화와 같은 정의를 사용하도록 `분석산출물/pkgC_operations/pkgC_optimization_v2.py`의 입력 계산을 재사용했습니다.
- 공급/인구: `외부데이터/시군구_수급_자연휴양림.csv`의 인구와 자연휴양림 수용인원을 17개 시도로 합산합니다.
- 수요: `DATA_2024_산림휴양복지활동조사_공표용_결측열제거.parquet`에서 Q20_5 시설 의향이 있고 Q20_2 이용 경험이 없는 응답자의 WT 가중 비율입니다. 개인 GAP 모델의 예측을 합산한 값은 아닙니다.
- 공급 조정 원점수: `z(미충족잠재수요율) + z(-log1p(수용인원 / 인구 × 100000))`. z의 표준편차는 원본과 같은 ddof=1입니다.
- 표시/평가 지수: 원점수 min–max 0–100, 소수 첫째 자리. 목적함수: 원점수를 다시 표준화한 z의 양수 부분.
- 보조 접근성: `본선_준비/experiments/results/regional_supply_indicators.csv`의 최근접 시설 직선거리. 이용 수준: 동일 원자료의 WT 가중 시설 경험률.
- 수요/공급 높음·낮음은 시도 중앙값 대비 안내입니다. 정책 문구는 지표에 적용한 해석 규칙이며 학습한 정책 예측값이 아닙니다. 자연휴양림 수용력 하나로 모든 시설 유형의 공급 충분도를 단정하지 않습니다.

다른 supply-adjusted 후속 산출물도 있지만 최종 운영 최적화와 산식이 다른 지수를 섞지 않았습니다.

## D. 최종 최적화 코드

확인 코드: `분석산출물/pkgC_operations/pkgC_optimization_v2.py`.

- 실측 예산: `외부데이터/(증빙) 수입지출 세부현황(2023년).xlsx`, 숲체험교육사업 2023년 결산 **6,321백만원 = 63.21억원**.
- 회당 비용: 결산 ÷ `기관별_2023_참여인원.csv`의 570,956명 × 정원 300명 = **3,321,271.69원**. 직접 관측한 프로그램 1회 단가가 아닌 역산 평균입니다.
- 결정변수: 지역 운영횟수 x(정수), 충족인원 y(연속).
- 목적함수: `maximize Σ(max(z_r,0) × y_r)`.
- 제약: `y_r ≤ 인구_r × 목표율`, `y_r ≤ 정원 × x_r`, `Σ(단가 × x_r) ≤ 예산`, x/y 비음수.
- 기본 목표율 1%. 인력 155 제한과 지역별 60회 제한은 없습니다. 필요 인력-회차는 반환 운영횟수 × 2인 가정입니다.
- 기준선: 같은 총 운영횟수에 대한 인구비례 / `param_reality_check.csv`의 기존 2023년 참여비례. 원본 최대잔여법의 동점 처리 순서도 보존했습니다.
- 평가: 세 방식 모두 동일한 표시용 0–100 지수 × 충족인원의 합을 비교합니다. 목적함수와 평가 지표는 서로 구분합니다.

| 기본 1% 조건 | 재계산 결과 |
|---|---:|
| 운영횟수 | 1,208회 |
| 예산 사용률 | 63.4725% |
| 인구비례 대비 개선 | +25.8001% |
| 기존 참여비례 대비 개선 | +646.2418% |
| 필요 인력-회차 | 2,416 |

이 값은 앱의 반환값에서 계산합니다. 기존 `optimization_v2_scenarios.csv`의 0.3/0.5/1.0% 시나리오와 일치하는지 검사했습니다. 직원 수가 제약에 없으므로 실제 인력 부족을 이 결과만으로 입증하지 않습니다.

## E. 바로 사용할 수 있었던 기존 파일

전처리 X/Y/meta, GAP/시설 성능 CSV와 OOF, 최종 최적화 시나리오/지역별 배분 CSV, 공급 CSV, 예산 XLSX, 실제 참여 자료, 변수 코드북, 원자료의 관측 지역코드가 있었습니다. 원본 전체 경로와 SHA-256은 `data/provenance.json`, 모델 학습 입력 해시는 각 `schema.json`의 sources에 기록했습니다.

## F. 새로 추출·저장한 파일

- `train_models.py` → GAP 6개 + 시설 13개 `.cbm`, 열 순서/범주/결측 규칙의 schema, 파일 해시 manifest. 저장 전후 예측 일치 검사 완료.
- `extract_data.py` → `regional_index.csv`, `optimization_input.csv`, `optimization_result.csv`, `optimization_constants.json`, `region_codes.csv`, 성능·라벨·출처 자료, 실제 동일 응답자의 전체 설문 시연 행.
- 시군구 명칭은 기존 로컬 `sgg_geo.json`의 조사코드와 매칭했습니다. 파일 부재 시 관측 조사코드를 표시합니다. **읍면동 명칭표는 현재 프로젝트 파일에서 찾지 못해 명칭을 만들지 않고 코드로 표시합니다.**
- 앱은 저장된 파일만 읽습니다. 원본 경로는 재생성 때만 필요합니다. 상세 설치·시연·검증 방법은 `README.md`를 따릅니다.
